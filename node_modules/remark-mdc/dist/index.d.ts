import { Plugin } from 'unified';

declare function stringifyFrontMatter(data: any, content?: string): string;
declare function parseFrontMatter(content: string): {
    content: string;
    data: Record<string, any>;
};

declare module 'unist' {
    interface Data {
        hName?: string;
        hProperties?: Record<string, any>;
    }
}
interface ComponentHanlder {
    name: string;
    instance: any;
    options?: any;
}
interface RemarkMDCOptions {
    components?: ComponentHanlder[];
}
declare const _default: Plugin<RemarkMDCOptions[]>;

export { _default as default, parseFrontMatter, stringifyFrontMatter };
